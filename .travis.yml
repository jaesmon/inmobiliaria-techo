language: node_js
sudo: false
node_js:
  - "8.9"
install:
  - npm install
script:
   - ng build --prod
branches:
  only:
  - master
  - gh-pages     # test the gh-pages branch
  - /pages-(.*)/ # test every branch which starts with "pages-"   


